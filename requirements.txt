install/import pygame # pip3 install pygame
# Pygame is a set of Python modules designed for writing video games.
    # includes computer graphics and sound libraries designed to be used
    # with the Python programming language

install/import os
# The OS module in python provides functions for interacting with the operating system.

install/import random
# The random() provides access to functions that support these types of operations. 

install/import time
# The time() function returns the number of seconds passed 